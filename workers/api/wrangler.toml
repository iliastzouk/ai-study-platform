name = "tzoukai"
main = "src/index.ts"
compatibility_date = "2025-02-24"
compatibility_flags = ["nodejs_compat"]
preview_urls = false

[vars]
# Non-secret config (override per environment below)
# Secrets are set via: wrangler secret put OPENAI_API_KEY etc.
# CORS_ALLOWED_ORIGINS is set per environment

[env.production]
name = "tzoukai-prod"
